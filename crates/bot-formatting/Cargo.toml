[package]
name = "housebot-bot-formatting"
version = "0.1.0"
edition = "2021"
rust-version = "1.80"
description = "Pure formatting helpers for Discord responses and tool progress"
license = "AGPL-3.0-or-later"

[dependencies]
regex = "1"
serde_json = "1"
