[package]
name = "housebot-testing"
version = "0.1.0"
edition = "2021"
rust-version = "1.80"
description = "Scripted ChatClient fake for tests"
license = "AGPL-3.0-or-later"

[dependencies]
anyhow = "1"
async-trait = "0.1"
housebot-llm = { path = "../llm" }
serde_json = "1"
